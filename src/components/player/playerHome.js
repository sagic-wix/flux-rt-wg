define(['react', 'lodash', './playerHome.rt'], function (React, _, template) {
    'use strict';

    return React.createClass({
        displayName: 'playerHome',
        render: template
    });
});
