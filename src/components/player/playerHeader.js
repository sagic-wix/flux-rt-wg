define(['react', 'lodash', './playerHeader.rt'], function (React, _, template) {
    'use strict';

    return React.createClass({
        displayName: 'playerHeader',
        render: template
    });
});
