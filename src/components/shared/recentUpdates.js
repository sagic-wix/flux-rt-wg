define(['react', 'lodash', 'parse', './recentUpdates.rt'], function (React, _, Parse, template) {
    'use strict';

    return React.createClass({
        displayName: 'recentUpdates',

        render: template
    });
});
