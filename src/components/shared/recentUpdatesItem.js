define(['react', 'lodash', './recentUpdatesItem.rt'], function (React, _, template) {
    'use strict';

    return React.createClass({
        displayName: 'recentUpdatesItem',
        render: template
    });
});
