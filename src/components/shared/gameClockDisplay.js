define(['react', 'lodash', './gameClockDisplay.rt'], function (React, _, template) {
    'use strict';

    return React.createClass({
        displayName: 'gameClockDisplay',
        render: template
    });
});
